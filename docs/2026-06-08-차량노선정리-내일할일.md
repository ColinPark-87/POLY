# 차량 노선 정리 — 이어서 할 일 (2026-06-08 저장, 내일 진행)

## 지금까지 (✅완료·배포됨)
- 정류장명 통일: 한진그랑빌 표준화 + 전 캠퍼스(중계/대치/정발) 공백·띄어쓰기 변형 통일. 데이터 무손상(배차/시간 변경 0건, 백업 크로스체크 확인).
- 호차 명단 카드 시간 자동매칭(세션그룹 한정 — 유치부에 초등부 섞임 수정).
- **호차별 인원 요일별 표시**: 명단 카드에 요일 토글(기본 오늘) + 정원 경고를 '하루 최대 인원'(busDayMaxCount) 기준으로. (commit 8e034a1)
- 백업: `_archive/backups/` (RouteMapView_v8~v11, page_v2, 정류장명 백업 JSON 3종)

## 핵심 진단 결과 (유치부 하원 2호차)
- **배정인원은 깨지지 않았음**: 백업 532건 크로스체크 = 호차배정·시간 변경 0건. 정류장명 통일은 인원에 영향 없음(busStudentCount는 busMap 기준).
- "23/21명"은 **주간 합집합**. 하루 최대=금 16명, 정원 17 이내. 요일별로 보면 정상.
- 김주안·김태안 = **쌍둥이(다른 학생 2명)**. 현재달(2026년 6월)엔 둘 다 신안동진 하나로 깔끔(15:00). "동진신안A"·"소문난감자탕(중앙하이츠A)"는 **과거 달 잔재**(라이브 아님). 학생 총 enrollment 12개는 과거 달 누적.
- **현재달 유치부 하원 2호차 = 7정류장, 전부 좌표 있음, 주간 22명.**

## 내일 할 일 (둘 다)
### 1) 노선 페이지에도 요일 필터 추가
- 위치: `RouteMapView.tsx`, sidebarPage===1(노선) — 명단 카드의 `rosterDay`(요일 토글, 기본=오늘) 패턴 재사용.
- 노선의 정류장 그룹핑/표시/순서를 **선택 요일에 타는 학생만**으로 필터 → 하루치 동선만 그려 깔끔.
- 정류장 노드(`s.days.includes(요일)`)로 필터. 기존 per-day 헬퍼(busDayMaxCount 등) 참고.

### 2) 늦은-시간(outlier) 정리
- 증상: 같은 정류장에 다수와 동떨어진 시간 1~2명이 섞여, 시간순 노선이 그 정류장으로 되돌아옴(지저분).
  - 예(현재달 유치부 하원 2호차): 신안동진 대부분 15:00인데 일부 15:30 / 중계1동 주민센터 건너편 15:03+15:30 / 포레나노원A 후문 15:10+15:27.
- 할 일: 읽기전용으로 **같은 (호차·정류장)에서 대표시간과 크게 벗어난 학생 시간 추출** → 사용자 확인(실제 늦는 건지 오타) → 통일.
- 데이터 변경이므로 백업 선행 + 검증. 시간 매칭은 sameStop(공백무시) 사용.

## 메모/주의
- 진단은 읽기전용(서비스키 .env.local). 마이그레이션은 Supabase **select 기본 1000행 제한** → 반드시 페이지네이션(.range).
- 라이브 뷰는 현재달(targetMonth)만. 진단도 현재달 필터해야 정확(전체 달 긁으면 과거 잔재로 과장됨).
- 동명이인/쌍둥이 흔함(박서준=동명이인, 김주안·김태안=쌍둥이). student_id로 구분.
