# 2026-07-06 차량관리·개설반 현황 다건 수정 (세션 워크로그)

중계 원장 제보 기반 버그 수정 + 본사(HQ) 캠퍼스 해석 수정 + 개설반 요일별 시간 편집 신설.
전 건 `vehicle-remote-redesign` 브랜치, `vercel --prod` 직접 배포(https://poly-system.vercel.app).
데이터 정정은 서비스롤 스크립트 + 백업.

---

## [2026-07-06 #1] 요일반 학생 차량 배정 요일 clamp
- **증상**: 중계 2호차 하원 홍유나(초등부 월수금)가 월수금인데 화·목까지 배차됨(월화수목금 전부).
- **근본원인**: `BusStopSettingsView.addRider`/`reqAddRider`가 빈 정류장 첫 탑승자 추가 시 `days: [...DAYS]`(월화수목금 전부)를 보냄 → "초등부 월수금" 학생에게 화·목이 박힘. `add_rider` 소행(updated_by=null 일치).
- **수정**: `lib/utils/session-days.ts` `clampRideDaysToSession()` — 세션명이 월수금/화목이면 그 요일로 제한(매일반·유치부는 제한 없음). `route.ts` `add_rider` + `approve_change_request` 두 쓰기경로에 적용. 홍유나 7월 데이터도 월수금으로 정정.
- **커밋/배포**: `f304653` → `poly-h4e6prfsx`. 백업 `backup/pre-hong-daybug-20260706`.

## [2026-07-06 #2] 정류장 후보 dedup을 세션 그룹별로
- **증상**: 개설반 현황 학생 차량편집에서, 유치부 김건우를 매일반 하원시간으로 배차하려는데 7호차 정류장이 상명·창동주공19만 뜨고 롯데우성·건영3차가 안 뜸.
- **근본원인**: `class-roster/page.tsx` `getStopGroups`가 **전역 `seen` Set**으로 정류장 중복제거 → 현재 세션(유치부)이 공유 정류장(롯데우성·건영)을 선점해 다른 세션(매일반) 그룹에서 사라짐. 함수 주석의 "전 세션 선택 가능" 설계의도를 dedup이 깨뜨림.
- **수정**: dedup을 **세션 그룹 내부로 한정** → 공유 정류장은 각 세션 시간으로 그룹마다 노출. 지도(reg) 그룹만 세션정류장과 중복제거. 실데이터 검증: 매일반 그룹 [상명,창동] → [상명,창동,롯데우성아파트,건영3차(경로당)].
- **커밋/배포**: `dfd4f44` → `poly-2ofmdf9p1`. 백업 `backup/pre-stopgroup-dedup-20260706`.

## [2026-07-06 #3] 신규학생 정류장 세션-락 제거 + 데이터 감사
- **수정 A**: `NewStudentModal.getArr/DepStops`도 세션-락(현재 세션 정류장만) 제거 → 전 세션 union + 등록정류장 병합(#2와 동일 수준). `registeredStops` prop 추가.
- **수정 B(데이터 감사)**: #1 clamp 버그가 과거 프로덕션에서 낸 피해 전 캠퍼스 스캔 → 홍유나형 세션-외 실호차 오배차 **8명 정정**(대치 5: 박서하·권래아·최하이·문승우·조우진 / 광명 3: 이선준·김서인·조수아). "차량안탐" 표식은 유지. 재감사 0건. 원본 백업 `_backup_daybug_20260706.json`.
- **커밋/배포**: `1e28b19` → `poly-ijczchnzf`. 백업 `backup/pre-newstudent-stops-20260706`.

## [2026-07-06 #4] 본사(HQ) 캠퍼스 해석 — 선택 캠퍼스 우선
- **증상**: 본사 HQ로 차량 운행현황 볼 때 광교·수지 등 **모든 캠퍼스가 중계 데이터로** 뜸.
- **근본원인**: HQ 계정(Colin Park, terasz.colin@gmail.com)이 role=hq_admin인데 **campus_id=중계**로 세팅. 캠퍼스 해석 `campusId = profile.campus_id; if(!campusId && hq_admin) param`이 항상 중계를 잡고 선택 param(광교)을 무시.
- **수정**: `lib/utils/resolve-campus.ts` `resolveCampusId()` — hq_admin은 선택 param 우선(홈 campus_id 있어도), 일반 사용자는 자기 campus_id만(param 무시=타 캠퍼스 열람 차단, 보안 유지). 차량 뷰가 쓰는 전 엔드포인트 일괄 적용: vehicles(GET+POST)·stop-coords·registered-stops·students·place-spots·presence·class-roster/import.
- **부수**: 본사 계정 추가 도구 `scripts/create-hq-account.mjs`(role=hq_admin, campus_id=null). 이상곤 차장 계정 생성(jason.lee@esc.edu-poly.com).
- **커밋/배포**: `d19b40c` → `poly-60msc45xq`. 백업 `backup/pre-hq-campus-resolve-20260706`.

## [2026-07-06 #5] 위치 없는 당일 override로 인한 미탑승 표시
- **증상**: 호차별 정류장 2호차 하원 수락산 동물병원 홍유나가 월요일(월수금 타는 날)인데 인원 0·회색(안 타는 것처럼).
- **근본원인**: `add_rider`가 당일 재탑승 override를 **location 없이(null)** 생성. `BusStopSettingsView.ridesHere`는 override 존재 시 그날 정류장이 override.location과 일치해야 탑승 인정 → null이라 "수락산 동물병원"과 불일치 → 제외.
- **수정**: `ridesHere` — override에 location 없으면 주간 정류장 유지(호차만 일치 확인), location 있을 때만 이동으로 판단. `add_rider` override에 `location` 저장(향후 null 방지). 같은 원인의 전 학생 일괄 정상화.
- **라이브 검증**: 수락산 동물병원 인원 0→1, 홍유나 정상 표시, 하원 탑승 8→9명 확인.
- **커밋/배포**: `8aa91c5` → `poly-fq1hcjku5`. 백업 `backup/pre-override-noloc-20260706`.

## [2026-07-06 #6] 개설반 현황 모달 요일별 등하원 시간 편집 신설
- **증상**: 유치부 김건우를 특정 요일만 매일반 정류장·시간으로 하원하려는데 시간이 계속 유치부 시간으로 되돌아감.
- **근본원인**: 개설반 학생 모달에 **요일별 시간 편집칸이 없었음**. 요일별 호차·장소만 저장하고 시간은 유치부 공유 `_time` 유지. 읽기 우선순위도 `_time > {요일}_time`이라 유치부 시간이 항상 이김.
- **수정**: 모달에 요일별 등원/하원 시간 input 추가(호차 있는 요일만). 정류장 선택 시 시간이 모호하지 않으면 자동채움. 저장 `buildDirSchedule` — 시간 **XOR 불변식**(탑승 요일 시간 동일→공유 `_time`, 다르면 요일별 `{요일}_time`+`_time` 제거). 무변경 저장은 기존과 동치.
- **라이브 검증**: 시간 입력칸 정상 렌더 확인. 저장 로직 tsc·154테스트·실데이터(SR enrollment 화=16:50 정상 저장) 확인.
- **주의**: 김건우는 enrollment 다수(ECP7·ECP6·유치부 방과후 여러)라 화요일 배차가 반마다 흩어져 충돌(ECP6 화=8호차 vs SR 화=7호차 16:50). enrollment 정리 필요(원장 진행).
- **커밋/배포**: `828b361` → `poly-614os6vsi`. 백업 `backup/pre-roster-perday-time-20260706`.

---

## 신규 파일/도구
- `lib/utils/session-days.ts` — `clampRideDaysToSession`(요일반 배차 요일 제한)
- `lib/utils/resolve-campus.ts` — `resolveCampusId`(hq_admin param 우선)
- `scripts/create-hq-account.mjs` — 본사 계정 추가/승격(role=hq_admin, campus_id=null)
- `_backup_daybug_20260706.json` — clamp 데이터 정정 8명 원본(롤백용)
- 테스트: `tests/session-days.test.ts`, `tests/resolve-campus.test.ts` (전체 154/154)

## 미결/후속
- 김건우 다중 enrollment 화요일 충돌 정리(원장).
- 광명 학생명단 일부 누락 제보 → 원장이 재입력으로 해결.
- 개설반 현황 HQ(`/hq/roster`)의 class-roster GET은 hq param 경로 없음(별개, 미조치).
